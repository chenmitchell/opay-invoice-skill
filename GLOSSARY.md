# GLOSSARY.md — 台灣電子發票術語表

> 每條術語都有兩行：**一句白話解釋**，加上**什麼時候會遇到**。
> 本檔為非官方的個人整理，法律定義請以財政部與相關法規為準；技術欄位定義請以歐付寶官方技術文件為準。

**快速跳轉**：[發票號碼與字軌](#一發票號碼與字軌) ｜ [買受人與載具](#二買受人與載具) ｜ [開立與作廢](#三開立作廢與更正) ｜ [B2B 專屬](#四b2b-專屬) ｜ [離線發票](#五離線發票) ｜ [稅務](#六稅務相關) ｜ [角色與平台](#七角色與平台) ｜ [技術欄位](#八技術欄位)

---

## 一、發票號碼與字軌

### 字軌（Invoice Word / InvoiceTerm）
發票號碼前面那兩個英文字母，例如 `AB12345678` 的 `AB`。財政部把號碼以「字軌＋號碼區間」為單位配發給營業人。
**什麼時候會遇到**：第一次串接時必做的第一件事就是設定字軌（`AddInvoiceWordSetting`）。沒有可用字軌，`Issue` 會直接失敗。

### 配號（Number Assignment）
財政部核配給你的發票號碼區間，例如 `AB12340000` 到 `AB12349999` 共 10,000 張。
**什麼時候會遇到**：每個期別開始前要去財政部平台申請，再用 `GetGovInvoiceWordSetting` 查結果、`AddInvoiceWordSetting` 匯入歐付寶。

### 期別（Period / InvoiceYear + InvoiceTerm）
台灣發票以**兩個月**為一期，一年六期（1-2月、3-4月、…、11-12月）。每期的字軌是獨立的。
**什麼時候會遇到**：跨期開立時如果沒有先設定下一期字軌，會在期別交界那天直接開不出發票——這是最經典的踩雷。

### 剩餘張數 / 餘量（Remaining）
目前這組字軌還有幾張沒用掉。
**什麼時候會遇到**：`GetInvoiceWordSetting` 會回傳。強烈建議做成監控告警，門檻抓「尖峰時段兩天的開立量」。

### 字軌狀態（Word Status）
一組字軌可能是「未使用」「使用中」「停用」「已用完」等狀態，用 `UpdateInvoiceWordStatus` 切換。
**什麼時候會遇到**：新增字軌後**必須手動設成「使用中」**，很多人忘了這步，然後困惑為什麼還是開不出來。

### 隨機碼（RandomNumber）
發票上的四位數字，是財政部對獎與驗證用的。
**什麼時候會遇到**：`Issue` 成功後會回傳；離線發票則要你自己產生。列印與通知買受人時都要帶上。

### 空白未使用發票（Blank Invoice）
期別結束時還沒用掉的號碼。依規定要向財政部申報這些號碼「未使用」。
**什麼時候會遇到**：期末。`QueryBlankInvoiceList` 查詢、`BlankInvAutoUploadSetting` 設定自動上傳、`DownLoadBlankInvList` 下載清單。忘記處理會有申報問題。

---

## 二、買受人與載具

### 載具（Carrier）
存放電子發票的「容器」，讓消費者不用拿紙本也能對獎與查詢。
**什麼時候會遇到**：`Issue` 時的 `CarrierType` / `CarrierNum` 欄位。台灣消費者最常用手機條碼。

### 共通性載具
可以跨店家使用的載具，最常見的是**手機條碼**（`/` 開頭共 8 碼）與**自然人憑證條碼**（2 碼英文＋14 碼數字）。
**什麼時候會遇到**：消費者結帳時報手機條碼。開立前**務必先用 `CheckBarcode` 驗證**，格式對但不存在的條碼會讓發票開失敗或歸戶失敗。

### 非共通性載具（會員載具）
店家自己的會員載具，只能在該店家的體系內使用。
**什麼時候會遇到**：做會員系統時。歸戶邏輯與共通性載具不同，消費者換手機或忘記帳號就找不到發票。

### 顯碼 / 隱碼
發票通知或列印時，載具號碼與買受人資訊要不要完整顯示。隱碼會遮蔽部分字元。
**什麼時候會遇到**：`InvoicePrint`、`InvoiceNotify` 的參數，以及個資保護要求。**對外顯示一律建議隱碼。**

### 捐贈碼 / 愛心碼（LoveCode）
消費者把發票捐給社福團體時填的代碼，通常 3 到 7 碼數字。
**什麼時候會遇到**：`Issue` 時 `Donation` 設為捐贈，並填 `LoveCode`。開立前**務必先用 `CheckLoveCode` 驗證**——愛心碼會被停用，用了停用的碼會開立失敗。

### 買受人統一編號（CustomerIdentifier）
公司行號的 8 位數統編。填了統編就不能用載具、不能捐贈。
**什麼時候會遇到**：B2C 開給公司時，或整個走 B2B 時。開立前建議用 `GetCompanyNameByTaxID` 驗證，避免統編錯誤導致對方無法扣抵。

---

## 三、開立、作廢與更正

### 開立（Issue）
產生一張電子發票，並由加值中心在期限內上傳財政部。
**什麼時候會遇到**：訂單成立、收款完成時。**注意 B2C 上傳期限 48 小時、B2B 7 天。**

### 延遲開立 / 預約開立（DelayIssue）
先把發票資料送給歐付寶，指定未來某個時間點才真正開立。
**什麼時候會遇到**：預購、訂閱、貨到才開發票的情境。可用 `TriggerIssue` 提前觸發、`CancelDelayIssue` 取消。

### 作廢（Invalid）
整張發票失效。**發票只能作廢，不能刪除。**
**什麼時候會遇到**：開錯、訂單取消。有法定期限限制，逾期就只能走折讓。作廢**不可復原**。

### 折讓（Allowance）
發票本身仍然有效，但金額部分退回，等於「銷貨退回或折讓證明單」。
**什麼時候會遇到**：部分退貨、價格調整。分「一般開立（紙本開立）」與「線上開立（通知開立）」兩種。

### 一般開立折讓 vs 線上開立折讓
**一般開立**：賣方單方面開立，立即生效。
**線上開立**：系統通知買受人，**需要買受人同意**才生效。
**什麼時候會遇到**：選 `Allowance` 或 `AllowanceByCollegiate`。選錯的後果是「以為折讓完成了，其實還在等對方按確認」。

### 作廢折讓（AllowanceInvalid）
把已經開立的折讓單作廢掉。
**什麼時候會遇到**：折讓也開錯的時候。線上折讓要用 `AllowanceInvalidByCollegiate` 取消。

### 註銷重開（VoidWithReIssue）
一個動作同時完成「作廢原發票」與「開立新發票」，維持前後關聯。
**什麼時候會遇到**：買受人資訊填錯（統編打錯、載具錯）需要重開時。比「先作廢再開立」安全，因為不會出現中間狀態。

### 發票通知（InvoiceNotify）
用 Email 或簡訊把發票資訊寄給買受人。
**什麼時候會遇到**：開立成功後。可用 `InvoiceNotifySetting` 設定要不要自動發送。

---

## 四、B2B 專屬

### B2B / B2C
**B2C**：賣給一般消費者，買受人可以用載具、可以捐贈。
**B2B**：買賣**雙方都是營業人**，必帶統一編號，沒有載具與捐贈。
**什麼時候會遇到**：決定要串哪一套 API。路徑前綴不同（`/B2CInvoice` vs `/B2BInvoice`），欄位也不同。

### 存證模式（ExchangeMode = 存證）
賣方開立後直接上傳財政部存證，**不需要買方確認**。
**什麼時候會遇到**：買方沒有電子發票交換能力，或雙方約定不走交換時。

### 交換模式（ExchangeMode = 交換）
每個動作都要對方確認：`Issue` → `IssueConfirm`、`Invalid` → `InvalidConfirm`、`Reject` → `RejectConfirm`、`Allowance` → `AllowanceConfirm`、`CancelAllowance` → `CancelAllowanceConfirm`。
**什麼時候會遇到**：與有電子發票系統的企業客戶往來。**只做開立不做確認，是 B2B 最常見的半套整合**——對方會永遠停在「等待確認」。

### 退回（Reject）
買方拒絕接受賣方開立的發票，把它退回去。
**什麼時候會遇到**：品項、金額、統編有誤時。B2C 沒有這個概念。退回後也要 `RejectConfirm`。

### 交易對象（Merchant Customer）
B2B 中你往來的另一家公司，需要事先在系統中維護基本資料。
**什麼時候會遇到**：`MaintainMerchantCustomerData`。沒先維護就開立，會找不到對象。

---

## 五、離線發票

### 離線電子發票
在**沒有網路**的環境下先開立發票，恢復連線後再上傳。
**什麼時候會遇到**：POS 機、行動攤位、展場、山區門市。路徑前綴仍是 `/B2CInvoice`（不是 `/OfflineInvoice`）。

### 取號 / 預先取號
連網時先向歐付寶領一批發票號碼到本機，離線開立時從本機號碼池取用。
**什麼時候會遇到**：離線發票的必要前置步驟。三種取號方式：自動配發（`WithAutoSplit`）、指定區間（`GetOfflineInvoiceWordSetting`）、依數量（`...Number`，會一併回傳隨機碼與加密資料）。

### 機台編號（POS ID / MachineNo）
每一台會開立發票的實體機器都要註冊一個編號，讓號碼區間彼此不重疊。
**什麼時候會遇到**：`OfflineMerchantPosSetting` 註冊、`QueryOfflineMerchantPosSetting` 查詢。多台機器共用同一批號碼會開出重號發票。

### 上傳（OfflineIssue / OfflineInvalid）
把離線開立或作廢的資料補送回歐付寶，再由歐付寶轉送財政部。
**什麼時候會遇到**：恢復連線後。**有 48 小時期限**，超過會有申報問題。

---

## 六、稅務相關

### 課稅別（TaxType）
發票的稅務分類，常見有應稅、零稅率、免稅。
**什麼時候會遇到**：`Issue` 的必填欄位。填錯會影響稅額計算與申報。

### 零稅率（Zero Tax Rate）
稅率為 0% 但仍屬課稅範圍，主要是外銷。與「免稅」在稅務上意義不同。
**什麼時候會遇到**：外銷、國際服務。通常還要填**通關方式**。

### 通關方式（Clearance Mark）
零稅率發票要註明是「經海關出口」還是「非經海關出口」。
**什麼時候會遇到**：`TaxType` 選零稅率時才需要填。

### 特種稅額 / 特種稅率（SpecialTaxType）
適用特種營業（例如夜總會、酒家、部分金融業）的稅率分類。
**什麼時候會遇到**：只有特定行業會用到。一般電商填一般應稅即可。

### 課稅別混合（Mixed Tax Type）
同一張發票裡有不同課稅別的品項。
**什麼時候會遇到**：賣應稅商品又賣免稅商品時。規格對混合開立有額外限制，**建議拆單開立**避免爭議。

### 含稅 / 未稅（VAT 欄位）
品項單價與金額是「含稅」還是「未稅」，由旗標欄位決定。
**什麼時候會遇到**：`Issue` 的金額欄位。這是**四捨五入誤差**最常出現的地方，一定要與財會確認。

---

## 七、角色與平台

### 加值中心（Value-Added Center, VAC）
財政部認可的電子發票服務業者，代營業人上傳發票資料。**歐付寶就是加值中心之一。**
**什麼時候會遇到**：整個串接的對象就是加值中心。你不會直接打財政部的 API。

### 財政部電子發票整合服務平台
政府的電子發票總平台，負責配號、存證、對獎、查詢。
**什麼時候會遇到**：申請字軌配號、授權加值中心、設定接收方式。**沒完成授權，所有 API 都不會動。**

### 特店 / 商店（Merchant）
在加值中心註冊的商家，識別碼是 `MerchantID`。
**什麼時候會遇到**：每次 API 呼叫的外層必填欄位。**測試環境與正式環境的 MerchantID 不同，且必須與金鑰成對。**

### 平台商（PlatformID）
代管多個特店的服務商，例如電商平台。一般廠商此欄留空。
**什麼時候會遇到**：只有平台商需要填。一般廠商誤填會直接失敗。

### 廠商後台（Vendor Portal）
歐付寶的管理介面：正式 <https://vendor.opay.tw>、測試 <https://vendor-stage.opay.tw>。
**什麼時候會遇到**：申請正式金鑰、輪換金鑰、查詢發票、人工維護。**金鑰外洩時第一時間就是來這裡輪換。**

---

## 八、技術欄位

### HashKey / HashIV
AES-128 的金鑰與初始向量，各 16 個 ASCII 字元，直接當 raw bytes 用。
**什麼時候會遇到**：每次加解密。**只能放 `.env`，嚴禁 commit、嚴禁進前端 JS/HTML/CSS。**

### `Data` 欄位
請求與回應中真正的內容，經過「URLEncode → AES-128-CBC/PKCS7 → Base64」處理。
**什麼時候會遇到**：每一支 API。**這裡沒有 `CheckMacValue`**——那是綠界 ECPay 的做法。

### `RqHeader.Timestamp`
Unix timestamp，**驗證區間只有 10 分鐘**。
**什麼時候會遇到**：主機時間沒校正時，所有 API 都會失敗且錯誤訊息很難懂。請確認 NTP 有在跑。

### `TransCode`（外層回應碼）
`1` 代表外層資料（`MerchantID` / `RqHeader` / `Data`）接收成功。
**什麼時候會遇到**：解密前就要先檢查。失敗通常是校時、金鑰配對、JSON 格式。

### `RtnCode`（內層回應碼）
解密後才看得到，`1` 代表業務處理成功。
**什麼時候會遇到**：**兩層都要檢查**。只檢查 `TransCode` 是常見 bug——外層成功但發票沒開出來。

### `RelateNumber`（訂單編號）
你自己系統的訂單識別碼，會被記在發票上。
**什麼時候會遇到**：開立時填。**逾時後用它來查詢「到底開了沒」**，是冪等性的關鍵。務必保證唯一。

### URLEncode（.NET 慣例）
歐付寶採用 .NET 的編碼慣例：空格編成 `+`（不是 `%20`），`!` `*` `(` `)` 不編碼。
**什麼時候會遇到**：加密的第二步。用錯慣例會得到與官方向量不同的密文，且**不會噴任何錯誤**。見 [`references/urlencode-table.md`](references/urlencode-table.md)。

### 冪等性（Idempotency）
同一個操作重複執行，結果應該與執行一次相同。
**什麼時候會遇到**：處理逾時重試時。**開立／作廢／折讓／註銷重開都不是冪等的**，重送可能開出兩張發票。詳見 [`guides/22-idempotency-and-retry.md`](guides/22-idempotency-and-retry.md)。

---

## 常見混淆對照

| 容易搞混的 | 差在哪 |
|---|---|
| **歐付寶 O'Pay** vs **綠界 ECPay** vs **歐買尬 OMG** | 三家不同公司，API 完全不相容 |
| **作廢** vs **折讓** vs **註銷重開** | 整張失效 / 部分退款 / 作廢並重開 |
| **存證模式** vs **交換模式** | 不用對方確認 / 每步都要確認 |
| **零稅率** vs **免稅** | 稅率 0% 但仍課稅範圍 / 不在課稅範圍 |
| **共通性載具** vs **非共通性載具** | 跨店通用 / 只在單一店家 |
| **`TransCode`** vs **`RtnCode`** | 外層傳輸 / 內層業務 |
| **`MerchantID`** vs **`PlatformID`** | 特店 / 平台商（一般廠商留空） |
| **字軌** vs **配號** vs **期別** | 兩個英文字母 / 號碼區間 / 兩個月為一期 |
| **B2C 48 小時** vs **B2B 7 天** | 上傳財政部的期限不同 |

---

> 名詞若與官方文件用語不一致，以官方文件為準。發現錯誤或缺漏，歡迎開 issue（見 [`CONTRIBUTING.md`](CONTRIBUTING.md)）。
