# ROADMAP — 未來規劃

> 本專案由我一個人在下班時間維護。以下是**方向與優先順序**，**不是承諾的交付時程**。
> 想要哪一項先做？在對應的 issue 按 👍，或直接提 PR。

---

## 優先順序的判準

本專案的資源有限，會依這三個問題排序：

1. **會不會讓人開錯發票？** — 正確性問題永遠第一。
2. **有多少人會踩到？** — 常見情境優先於邊角案例。
3. **官方文件有沒有講清楚？** — 官方講不清楚的地方，補起來的價值最高。

---

## v1.1 — 穩固基礎

**目標：讓現有內容更可靠、更好維護。**

| 項目 | 說明 | 狀態 |
|---|---|---|
| CI 驗證腳本完備 | `scripts/` 補齊：API 覆蓋比對、Mermaid 色盤、內部連結、金鑰掃描 | 規劃中 |
| 內部連結全檢 | 所有 Markdown 內部連結與錨點自動驗證 | 規劃中 |
| 錯誤碼實測補充 | 收集使用者回報的實際錯誤碼與訊息，補進 `error-handling.md` | 持續進行 |
| 官方文件版本追蹤 | 官方文件改版時的差異比對流程與紀錄 | 規劃中 |
| guides 交叉引用檢查 | 確保每支 API 都至少被一份 guide 提到 | 規劃中 |

---

## v1.2 — 擴大語言覆蓋

**目標：讓更多技術棧的開發者可以直接複製使用。**

| 項目 | 說明 |
|---|---|
| **Go** client 模板 | 涵蓋 69 支 API，零外部相依（標準函式庫 `crypto/aes`） |
| **Java** client 模板 | Java 11+，含 Spring Boot 整合範例 |
| **C#** client 模板 | .NET 6+，加密部分可直接對應官方文件的 .NET 範例 |
| **Ruby** client 模板 | Rails 整合範例 |
| 各語言的自我測試 | 一律用 `test-vectors/aes-encryption.json` 驗證，不發網路請求 |

> 每個新語言都必須滿足 [`CONTRIBUTING.md`](CONTRIBUTING.md) 的「新增 client 語言的最低要求」。

---

## v1.3 — 更好的驗證工具

**目標：讓「我到底哪裡做錯了」變成三分鐘就能回答的問題。**

| 項目 | 說明 |
|---|---|
| CLI 診斷工具 | 單一指令跑完加密驗證、時差檢查、金鑰配對檢查、唯讀 API 連通性 |
| 請求／回應除錯器 | 貼上加密的 `Data` 與金鑰（本機執行、不上傳），還原明文並指出格式問題 |
| 測試主控台擴充 | 增加 B2B 與離線發票的自我驗證流程 |
| 欄位驗證器 | 依 `references/enums.md` 檢查請求 JSON 的列舉值與必填欄位 |
| Postman / Bruno collection | 可直接匯入的 API 集合（僅測試環境） |

---

## v1.4 — 營運與監控

**目標：把「上線之後才會遇到的事」講清楚。**

| 項目 | 說明 |
|---|---|
| 字軌餘量監控範本 | Prometheus metrics ＋ Grafana dashboard JSON |
| 對帳腳本範本 | 自家紀錄 vs `GetIssue` 查詢結果的差異比對 |
| 期別交界檢查清單 | 雙月交界前的自動化檢查與提醒 |
| 上傳期限監控 | B2C 48 小時、B2B 7 天的逾期預警 |
| 常見事故的 runbook | 字軌用完、加值中心逾時、大量開立失敗時的處置步驟 |

---

## v2.0 — 更廣的覆蓋（探索中）

這些方向還在評估，**不保證會做**：

| 項目 | 考量 |
|---|---|
| 其他加值中心的對照表 | 幫助從其他服務搬遷的人；但要小心不要變成推薦或比較 |
| 財政部平台操作圖解 | 授權加值中心、申請配號的步驟；需要大量脫敏截圖 |
| 英文版 | 台灣的外商團隊有需求；但維護雙語成本高 |
| MCP server | 把規格查詢做成 MCP tool，讓 AI 可以結構化檢索而非全文檢索 |
| 互動式教學 | 瀏覽器中就能跑的加密實驗場 |

---

## 明確不做的事

為了維持專案定位清楚，以下**不會做**：

| 不做 | 原因 |
|---|---|
| ❌ 提供正式環境的代管服務或 API proxy | 本專案是文件與模板，不是服務 |
| ❌ 收費、pro 版、廣告、導流 | 本專案定位為公共財，見 [`MANIFESTO.md`](MANIFESTO.md) |
| ❌ 宣稱法規符合性或提供法律／稅務意見 | 超出一個人維護的專案的能力與責任範圍 |
| ❌ 收集使用者資料或 telemetry | 隱私優先 |
| ❌ 比較「哪家加值中心比較好」 | 容易變成商業推薦，也非本專案專長 |
| ❌ 代為向歐付寶申請或協調 | 請直接聯繫官方窗口 |

---

## 怎麼影響優先順序

1. **開 issue 描述你的情境**——「我遇到什麼問題、目前怎麼繞過」比「請加 X 功能」有用得多。
2. **在既有 issue 按 👍**——這是最直接的優先度訊號。
3. **直接提 PR**——本專案對 PR 的態度是「先合再優化」，只要正確、脫敏、符合規範。
4. **分享踩坑經驗**——這是本專案最需要、也最難自己產出的內容。

詳見 [`CONTRIBUTING.md`](CONTRIBUTING.md)。
